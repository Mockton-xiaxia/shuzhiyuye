# 17 — 全栈自检报告（2026-07-25）

> 接续人工走查 + 脚本冒烟，不重复 `11-全量验收记录` 明细表。

## 1. 自动化结果（本轮重跑）

| 项 | 结果 |
|----|------|
| `scripts/accept_full.py` | **PASS**，`fail_count=0` |
| `frontend/apps/admin` `npm run build` | **成功**（有大 chunk 体积告警，非失败） |
| `backend/test_gis_utils.py` | 修复校验误报后应 **通过** |
| `pytest` | 未纳入 `uv` 依赖，仅脚本式自测 |

## 2. 已发现并处理的问题

| 严重度 | 问题 | 处理 |
|--------|------|------|
| **高** | `GeometryUtils.validate_polygon` 对正常矩形误报「自相交」，绘面保存可能被拒 | 已修：端点相接不算相交 + 跳过闭合边误判 |
| **中** | `test_gis_utils.py` 校验失败仍打印「全部通过」 | 已修：`valid=False` 时 `SystemExit` |
| **中** | 渔业资源统计页「定位」只飞点、视野丢其它要素 | 已在 `GisMapPage.vue` 改为 fit 几何 + 查看全部 |
| **低** | Windows 控制台 GBK 下 emoji 导致脚本乱码 | 运行时用 `PYTHONIOENCODING=utf-8` |

## 3. 文档与契约不一致（建议改文档，非阻塞）

| 项 | 说明 |
|----|------|
| `00-详细设计索引.md` | 未收录 `11–18`、`14–17` 等后续文档；文首仍写 PostgreSQL，与根目录 `README`（默认 SQLite）不一致 |
| `11-全量验收记录` | 「渔业资源统计」登记 API 为 `/analytics/resources`，**页面实际**走 `GisMapPage` → `/party/gis/layers?mode=stats`；验收只测 catalog API，易误解 |
| `12-残留缺口` | 驾驶舱/GIS 段落偏旧，未反映 kjxx 对齐、Overlay 标签、资源 141 张等近期工作 |
| `18-前端技术栈分析.md` | 多处代码块 fence 损坏（`` `ue ``），Markdown 无法正常渲染 |
| `16-深度爬取` | 大屏示例仍写 `#/yzsc` 为企业场景，与区县 `#/home` 需对照 `13` 区分 |

## 4. 产品/实现仍存在的真实差距（与 `12` 一致部分）

- 萤石 EZUIKit 真直播、SSO 票据联登、设备下发仍为 mock/壳
- 大量菜单仍是通用 CRUD，非现网全 Tab/级联/OSS
- 区县驾驶舱：FanDiagram / DotRing / WaveBall 等为 ECharts 近似，乡镇面级 GeoJSON 下钻未接官方边界
- 前端首包约 1.2MB+（index chunk），生产建议按路由 lazy + manualChunks
- 仓库根目录 **无 git**，版本追溯需自备（或后续 init）

## 5. 建议优先级（下一步）

1. **P0**：绘面/审核链路再测一遍（矩形、凹多边形、自相交真例）— 依赖已修校验
2. **P1**：同步更新 `00` 索引 + `12` 缺口 + `11` 中 gis-stats 的 API 说明
3. **P1**：修复 `18` 的 Markdown 代码 fence
4. **P2**：验收脚本增加「页面真实 API」对照（catalog `api` vs 组件内 `http.get`）
5. **P2**：驾驶舱/大屏分包与 lazy route

## 6. 快速复验命令

```powershell
$env:PYTHONIOENCODING='utf-8'
cd d:\ai\domo\yuye\backend
uv run python test_gis_utils.py
uv run python ..\scripts\accept_full.py

cd d:\ai\domo\yuye\frontend\apps\admin
npm run build
```
